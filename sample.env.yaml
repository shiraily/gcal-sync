url: https://example.com/notify
project: project-name

src: src_token.json
dest: dest_token.json

# (not implemented yet) if you want to use Service Account only, specify calendar id
# src: hoge@example.com
# dest: fuga@group.calendar.google.com
rules:
  - match: "病院"
    start_offset: -30
    end_offset: 30
  - match: "整体"
    start_offset: -30
    end_offset: 30
