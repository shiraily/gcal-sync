url: https://example.com/notify
project: project-name
cal_id_private: hoge@example.com
cal_id_business: fuga@group.calendar.google.com
client_secret: '{
  "type": "service_account",
  "project_id": "project-id",
  ...
}'
rules:
  - match: "病院"
    start_offset: -30
    end_offset: 30
  - match: "整体"
    start_offset: -30
    end_offset: 30
