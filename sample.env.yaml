url: https://example.com/notify
project: project-name

src: hoge@example.com
dest: fuga@example.com

# if you want to use OAuth client, specify token file
# src_token_file: src_token.json
# dest_token_file: dest_token.json

rules:
  - match: "病院"
    start_offset: -30
    end_offset: 30
  - match: "整体"
    start_offset: -30
    end_offset: 30
