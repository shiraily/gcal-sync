url: https://example.com/notify
project: project-name
src_cal_id: hoge@example.com
dest_cal_id: fuga@group.calendar.google.com
client_secret: '{
  "type": "service_account",
  "project_id": "project-id",
  ...
}'
rules:
  - match: "病院"
    start_offset: -30
    end_offset: 30
  - match: "整体"
    start_offset: -30
    end_offset: 30
